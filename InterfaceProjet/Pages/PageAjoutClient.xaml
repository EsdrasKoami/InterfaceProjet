<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="InterfaceProjet.Pages.PageAjoutClient"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:InterfaceProjet.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer>
        <Grid Padding="40">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- TITRE -->
            <StackPanel Grid.Row="0"
                        Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        Spacing="10"
                        Margin="0,0,0,20">
                <SymbolIcon Symbol="Contact"/>
                <TextBlock Text="Ajout d'un client"
                           FontSize="28"
                           FontWeight="SemiBold"/>
            </StackPanel>

            <!-- CARTE FORMULAIRE -->
            <Border Grid.Row="1"
                    HorizontalAlignment="Center"
                    CornerRadius="12"
                    Padding="24"
                    BorderThickness="1"
                    BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                <StackPanel Spacing="12"
                            MaxWidth="600">

                    <StackPanel>
                        <TextBox x:Name="tbxNom"
                                 Header="Nom du client"/>
                        <TextBlock x:Name="tblErrNom"
                                   Foreground="Red"
                                   FontSize="12"/>
                    </StackPanel>

                    <StackPanel>
                        <TextBox x:Name="tbxAdresse"
                                 Header="Adresse"
                                 TextWrapping="Wrap"
                                 AcceptsReturn="True"
                                 Height="80"/>
                        <TextBlock x:Name="tblErrAdresse"
                                   Foreground="Red"
                                   FontSize="12"/>
                    </StackPanel>

                    <StackPanel>
                        <TextBox x:Name="tbxTelephone"
                                 Header="Numéro de téléphone"
                                 PlaceholderText="Ex : 819-563-4521"/>
                        <TextBlock x:Name="tblErrNumeroTelephone"
                                   Foreground="Red"
                                   FontSize="12"/>
                    </StackPanel>

                    <StackPanel>
                        <TextBox x:Name="tbxEmail"
                                 Header="Email"
                                 PlaceholderText="exemple@domaine.com"/>
                        <TextBlock x:Name="tblErrEmail"
                                   Foreground="Red"
                                   FontSize="12"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal"
                                HorizontalAlignment="Right"
                                Spacing="8"
                                Margin="0,10,0,0">
                        <Button Content="Annuler"
                                Width="140"
                                Click="ButtonAnnuler_Click"/>
                        <Button x:Name="btnEnregister" Content="Enregistrer"
                                Width="160"
                                Click="ButtonEnregistrer_Click"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</Page>
