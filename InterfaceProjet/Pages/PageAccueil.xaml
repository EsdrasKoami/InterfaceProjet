<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="InterfaceProjet.Pages.PageAccueil"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:InterfaceProjet.Pages"
    xmlns:local1="using:InterfaceProjet.Classes"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel  Orientation="Horizontal" Spacing="8" Margin="0,30,0,0" HorizontalAlignment="Center">
            <TextBlock Text="Rechercher un projet"  VerticalAlignment="Center"/>
            <TextBox x:Name="tbRechercheProjet" PlaceholderText="Rechercher un projet par titre, numéro ou client..." Margin="10,0,0,0" />
        </StackPanel>
        <TextBlock Text="Projets en cours" Grid.Row="1" FontSize="25" FontFamily="Arial Black" Margin="0,20,0,0" />
        <GridView x:Name="listeProjetsEnCours" Grid.Row="2"   Margin="40,30,40,40" >
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="local1:Projet">
                    <StackPanel>
                        <TextBlock Text="{x:Bind NumeroProjet}" FontWeight="Bold"/>
                        <TextBlock Text="{x:Bind Titre}" />
                        <TextBlock Text="{x:Bind NomClient}" />
                        <TextBlock Text="{x:Bind DateDebut}" />
                        <TextBlock Text="{x:Bind Budget}" />
                        <TextBlock Text="{x:Bind Statut}" />
                    </StackPanel>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>

    </Grid>
</Page>
