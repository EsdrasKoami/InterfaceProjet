<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="InterfaceProjet.Pages.PageAjoutPojet"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:InterfaceProjet.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <ScrollView>
    <Grid>
        <Grid Padding="40">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0"
            Orientation="Horizontal"
            HorizontalAlignment="Center"
            Spacing="10"
            Margin="0,0,0,20">
                <SymbolIcon Symbol="Add"/>
                <TextBlock Text="Ajout d'un projet"                        
               FontSize="28"
               FontWeight="SemiBold"/>  
            </StackPanel>
            <StackPanel Grid.Row="1">
            
                <StackPanel>
                    <TextBox x:Name="tbxtitre" Header="Titre"/>
                    <TextBlock x:Name="tblErrTitre" Foreground="Red"/>
                </StackPanel>
                <StackPanel>
                    <DatePicker x:Name="dpDateDebut" Header="Date de debut" />
                    <TextBlock x:Name="tblErrDateDebut" Foreground="Red"/>
                </StackPanel>
                <StackPanel>
                    <TextBox x:Name="tbxDescription" Header="Description" PlaceholderText="Description du projet ....." TextWrapping="Wrap"/>
                    <TextBlock x:Name="tblErrDescription" Foreground="Red"/>
                </StackPanel>
                <StackPanel>
                    <NumberBox x:Name="nbxBudget" Header="Budget" />
                    <TextBlock x:Name="tblErrBudget" Foreground="Red"/>
                </StackPanel>
                <StackPanel>
                    <NumberBox x:Name="nbrEmploye" Header="Le nombre d'employés" />
                    <TextBlock x:Name="tblErrNbrEmploye" Foreground="Red"/>
                </StackPanel>
                <StackPanel>
                    <NumberBox x:Name="totalSalaire" Header="Total salaire à payer"/>
                    <TextBlock x:Name="tblErrTotalsalaire" Foreground="Red"/>
                </StackPanel>
                <StackPanel>
                    <TextBox x:Name="idClient" Header="Identifiant du client"/>
                    <TextBlock x:Name="tblErrIdClient" Foreground="Red"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Spacing="8">
                    <Button Content="Enregistrer" Click="ButtonEnregistrer_Click" Width="250"/>
                    <Button Content="Annuler" Click="ButtonAnnuler_Click" Width="250"/>
                </StackPanel>
            </StackPanel>
        </Grid>

    </Grid>
    </ScrollView>
</Page>
